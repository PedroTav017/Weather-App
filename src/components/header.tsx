import React from "react";

//styles
import styles from "../styles/components/header.module.css"

const Header = () => {
  return (
    <div className={styles.header}>
      <h2>Pedro's Weather App</h2>
    </div>
  );
};

export default Header;
